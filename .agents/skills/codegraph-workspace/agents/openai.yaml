interface:
  display_name: "CodeGraph Workspace"
  short_description: "Use CodeGraph indexes while coding"
  default_prompt: "Use $codegraph-workspace to inspect this indexed repo with CodeGraph before grep/read."
