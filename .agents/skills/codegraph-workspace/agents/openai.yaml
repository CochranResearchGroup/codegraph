interface:
  display_name: "CodeGraph Workspace"
  short_description: "Use CodeGraph indexes while coding"
  default_prompt: "Use $codegraph-workspace to check CodeGraph status, refresh stale indexes, then inspect this repo before grep/read."
